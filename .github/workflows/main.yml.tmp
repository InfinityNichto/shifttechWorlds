name: Main Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        max-parallel: [4, 4]

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Download World
      run: |
        

    - name: Install dependencies
      run: |
        # Add your dependency installation commands here

    - name: Main process step 1
      run: |
        # Main process step 1 commands here

    - name: Main process step 2
      run: |
        # Main process step 2 commands here

    - name: Main process step 3
      run: |
        # Main process step 3 commands here
